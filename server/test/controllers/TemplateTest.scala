package controllers

import org.scalatestplus.play.PlaySpec
import play.api.i18n.Lang
import play.api.mvc.{Flash, Session}
import play.api.test.Helpers._

class TemplateTest extends PlaySpec {
/*
  implicit val f = Flash()
  implicit val s = Session()
  implicit val m = new MockMessageApi().preferred(Seq(Lang("en")))
  "Templates" when {
    "return index" should {
      "contain menu links" in {
        val html = views.html.index()
        contentAsString(html) must (include("/task") and include("/addTask") and include("/register"))
      }
    }
  }
*/
}
